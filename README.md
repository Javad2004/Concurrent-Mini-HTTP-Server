# Concurrent-Mini-HTTP-Server
A concurrent HTTP server in C using worker processes, POSIX threads, round-robin scheduling, UNIX-domain socket IPC, and SCM_RIGHTS file-descriptor passing, with GET/POST support and a multithreaded stress client.
